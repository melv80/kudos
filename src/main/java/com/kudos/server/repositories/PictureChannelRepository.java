package com.kudos.server.repositories;

import com.kudos.server.model.jpa.PictureChannel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PictureChannelRepository extends JpaRepository<PictureChannel, Long> {
}
